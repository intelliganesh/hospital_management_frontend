// ProtectedRoute.jsx
// import { Navigate } from 'react-router-dom';

// const ProtectedRoute = ({ tokenData, children, navTo = '/login' }) => {
//   return tokenData ? children : <Navigate to={navTo} replace />;
// };

// export default ProtectedRoute;
